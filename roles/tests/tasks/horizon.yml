---

- name: Set Test Variables
  set_fact:
    group: "apache"
    host: "127.0.0.1"
    ports: "{{ horizon_ports }}"
    packages: "{{ horizon_packages }}"
    services: "{{ horizon_services }}"
    configuration_files: "{{ configuration_files_checked }}"

- include: common.yml
